{
  queryProduct(
    filterBy: {
      entityPrimaryKeyInSet: [1, 2, 3]
    },
    orderBy: [{
      attributeCodeNatural: DESC
    }],
    require: {
      priceType: WITH_TAX
    }
  ) {
    recordPage {
      data {
        primaryKey
      }
    }
  }
}