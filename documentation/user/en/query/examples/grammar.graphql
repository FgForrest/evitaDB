{
  queryProduct(
    filterBy: {
      entityPrimaryKeyInSet: [1, 2, 3]
    },
    orderBy: [{
      attributeCodeNatural: DESC
    }]
  ) {
    recordPage {
      data {
        primaryKey
      }
    }
  }
}