{
  queryProduct {
    extraResults {
      attributeHistogram {
        width {
          min
          max
          overallCount
          buckets(requestedCount: 5) {
            index
            threshold
            occurrences
            requested
          }
        }
        height {
          min
          max
          overallCount
          buckets(requestedCount: 5) {
            index
            threshold
            occurrences
            requested
          }
        }
      }
    }
  }
}