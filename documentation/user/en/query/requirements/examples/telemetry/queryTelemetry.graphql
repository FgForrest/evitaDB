{
  queryProduct(
    filterBy: {
      attributeCodeStartsWith: "garmin"
    },
    orderBy: [
      {
        attributeCodeNatural: ASC
      }
    ]
  ) {
    extraResults {
      queryTelemetry
    }
  }
}