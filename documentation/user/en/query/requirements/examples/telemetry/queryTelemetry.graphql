{
  queryProduct(
    filterBy: {
      attributeCodeStartsWith: "garmin"
    },
    orderBy: [
      {
        attributeCodeNatural: ASC
      }
    ]
  ) {
    recordPage {
      data {
        primaryKey
      }
    }
    extraResults {
      queryTelemetry
    }
  }
}