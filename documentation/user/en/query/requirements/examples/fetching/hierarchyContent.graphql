{
  queryCategory(
    filterBy: {
      attributeCodeEquals: "smartwatches"
    }
  ) {
    recordPage {
      data {
        primaryKey
        parentPrimaryKey
        parents {
          primaryKey
        }
      }
    }
  }
}