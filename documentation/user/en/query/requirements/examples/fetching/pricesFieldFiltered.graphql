{
  queryProduct(
    filterBy: {
      entityPrimaryKeyInSet: [
        103885
      ]
    }
  ) {
    recordPage {
      data {
        primaryKey
        attributes {
          code
        }
        prices(priceLists: "basic", currency: EUR) {
          priceList
          priceWithoutTax
          priceWithTax
          taxRate
        }
      }
    }
  }
}