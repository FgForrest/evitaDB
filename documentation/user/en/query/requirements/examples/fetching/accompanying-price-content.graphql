{
  queryProduct(
    filterBy: {
      attributeCodeEquals: "iget-blackview-tab-g11",
      priceInCurrency: EUR,
      priceInPriceLists: [
        "basic"
      ]
    }
  ) {
    recordPage {
      data {
        primaryKey
        priceForSale {
          priceWithoutTax
          priceWithTax
          taxRate
        }
      }
    }
  }
}