{
  queryBrand(
    filterBy: {
      entityPrimaryKeyInSet: [
        64703
      ]
    }
  ) {
    recordPage {
      data {
        primaryKey
        type
        version
        locales
        allLocales
      }
    }
  }
}