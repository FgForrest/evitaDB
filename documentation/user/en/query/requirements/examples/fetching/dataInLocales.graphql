{
  queryBrand(
    filterBy: {
      entityPrimaryKeyInSet: [
        64703
      ]
    }
  ) {
    recordPage {
      data {
        primaryKey
        attributes(locale: cs) {
          code
          name
        }
      }
    }
  }
}