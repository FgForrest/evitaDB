{
  queryProduct(
    filterBy: {
      entityPrimaryKeyInSet: [
        103885
      ]
    }
  ) {
    recordPage {
      data {
        primaryKey
        attributes {
          code
        }
        priceForSale(priceList: "employee-basic-price", currency: EUR) {
          priceWithoutTax
          priceWithTax
          taxRate
        }
      }
    }
  }
}