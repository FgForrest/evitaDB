=== JFR profile: attributeFiltering, FIXED build (SortIndex gate = getTransactionalMemoryLayerIfExists==null) ===
Recording: attrfilter-fixed-cpu.jfr | events: jdk.ExecutionSample --stack-depth 128 | total samples: 29200

=== Sort-cache fast path — CONFIRMED ENGAGED (was 0/17268 on the broken build) ===
buildCachedSupplier                            4      (0 -> 4: cache-build path now executes on read-only queries)
resolvePositionsByDenseWalk                    0      (4506 -> 0: the cold O(N) dense walk is ELIMINATED)
resolvePositionsBySparseProbe                 21      (cheap sparse probe replaced the dense walk)
getCachedAscendingArrays / getCachedDescendingArrays  0  (cache-hit accessors too cheap to sample; buildCachedSupplier proves the path runs)
SortIndexChanges.getAscendingOrderRecordsSupplier / ...Descending  0  (old slow path gone)

=== Throughput ===
attributeFiltering:              1998.6 +/- 209.4  ->  2237.6 +/- 524.1 ops/s   (within noise; filter-bound, see below)
attributeAndHierarchyFiltering:  5799.4 +/- 215.2  -> 10182.7 +/- 410.2 ops/s   (~1.76x, decisive; was dense-walk-bound)
(dev baseline: attributeFiltering 5858.4, attributeAndHierarchyFiltering 14879.5)

=== attributeFiltering's NEW dominant cost = the FILTER path, NOT the sort path ===
TOP SELF FRAMES (leaf), 29200 samples:
  6341  roaringbitmap.Util.fillArray(long[],char[])            <-- 100% from ArrayContainer.loadData(BitmapContainer)
  5972  java.lang.ThreadLocal.get()                            <-- 6126 from Transaction.getTransactionalMemoryLayerIfExists
  1966  SortedRecordsProvider.resolvePositions(...)            (the sort resolution itself, now cheap sparse probe)
  1764  roaringbitmap.BitmapContainer.computeCardinality()
  1303  roaringbitmap.Util.hybridUnsignedBinarySearch(...)
   898  roaringbitmap.Container.lazyIOR(Container)
   733  java.util.Arrays.fill(long[],long)
   714  query.algebra.AbstractFormula.initFields(Formula[])
   661  index.bitmap.RoaringBitmapBackedBitmap.getRoaringBitmap(Bitmap)

CALL CHAIN for the ThreadLocal tax (~21% of CPU), fully resolved:
  ThreadLocal.get()  (5972)
   <- Transaction.getTransactionalMemoryLayerIfExists           (6126)
   <- TransactionalBitmap.getRoaringBitmap()                    (6000)   -- always returns null layer in a read-only query
   <- RoaringBitmapBackedBitmap.getRoaringBitmap(Bitmap)        (6000)
   <- OrFormula.getRoaringBitmaps()                             (5951)   -- one ThreadLocal.get per OR'd attribute-value bitmap

=== TWO candidate optimizations for attributeFiltering (both SEPARATE from the SortIndex sort cache) ===
1. RoaringBitmap container materialization (~21%): ArrayContainer.loadData/fillArray. LIKELY the real
   #760-vs-dev regression driver (from #760's InvertedIndex / bitmap-storage changes). CONFIRM FIRST with a
   dev-vs-760 CPU-profile diff before editing the RoaringBitmap fork or InvertedIndex.
2. Transactional-bitmap ThreadLocal tax (~21%): pre-existing pattern; every TransactionalBitmap.getRoaringBitmap()
   does a ThreadLocal.get. Safe fix = hoist the per-thread transaction check out of the per-bitmap unwrap
   (e.g. in OrFormula, check Transaction.isTransactionAvailable() once and unwrap via a non-checking accessor
   when no transaction is bound). Cross-cutting; correctness hinges on "no transaction => no layer anywhere" (true).

Artifacts: attrfilter-fixed-results.json, hierarchy-fixed-results.json, attrfilter-fixed-cpu.jfr (this dir).
