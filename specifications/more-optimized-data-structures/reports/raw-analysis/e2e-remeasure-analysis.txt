=== JFR profile: attributeFiltering, correctly-built fixed jar (post-SortIndex-cache) ===
Recording: attrfilter-fixed-cpu.jfr | events: jdk.ExecutionSample | total samples:
17268

=== Fast-path (fix) frame occurrence counts — expect these to be the majority if the fix engages ===
buildCachedSupplier            0
getCachedAscendingArrays       0
getCachedDescendingArrays      0
getCachedAllRecordsBitmap      0

=== Old-path / cold-walk frame occurrence counts — expect these near-zero if the fix engages ===
SortIndexChanges.getAscendingOrderRecordsSupplier 0
SortIndexChanges.getDescendingOrderRecordsSupplier 1
resolvePositionsByDenseWalk                   4506
getOrCreateSortIndexChanges                   0

=== Example stack proving the OLD path is what actually runs ===
    io.evitadb.index.attribute.SortIndex.createReversedSortedComparableForwardSeeker() line: 907
    io.evitadb.index.attribute.SortIndexChanges.getDescendingOrderRecordsSupplier() line: 131
    ...
  ]
}

jdk.ExecutionSample {
  startTime = 21:33:50.090 (2026-07-09)

=== Conclusion ===
0 samples reach the new cache accessors (buildCachedSupplier / getCached*). The sampled stack above
shows SortIndexChanges.getDescendingOrderRecordsSupplier() actually executing — a frame reachable
ONLY on the pre-fix path, since the fast path builds the supplier directly and never routes through
SortIndexChanges at all. Its low raw count (1) and getOrCreateSortIndexChanges' count (0) reflect
JIT inlining of these small, hot accessor methods, not rarity of the call — resolvePositionsByDenseWalk
(4506 samples, the cold walk this fix exists to bypass) is the reliable signal that the old path
dominates. Root cause: the fast-path gate (Transaction.getTransactionalMemoryLayerIfExists(this) ==
null) is false on essentially every real query because SortIndex.getRecordsEqualToInternal() (line
1217, a read-only attribute-equality lookup used during filtering) and possibly other read paths call
the *creating* accessor getOrCreateSortIndexChanges() before the sort step's own gate check runs,
so a transactional layer already exists by the time the fast path is evaluated.
