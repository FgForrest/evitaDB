query(
    collection('Category'),
    filterBy(
        hierarchyWithinSelf(
            attributeEquals('code', 'audio')
        )
    ),
    require(
        hierarchyOfSelf(
            children(
                'directChildren',
                entityFetch(attributeContent()),
                stopAt(distance(1))
            ),
            parents(
                'directParent',
                entityFetch(attributeContent()),
                stopAt(distance(1))
            )
        )
    )
)