query(
    collection('Product'),
    filterBy(
        hierarchyWithin(
            'categories',
            attributeEquals('code', 'audio')
        )
    ),
    require(
        hierarchyOfSelf(
            children(
                'subcategories',
                entityFetch(attributeContent(code)),
                stopAt(distance(1))
            )
        )
    )
)