{
  queryProduct(
    filterBy: {
      hierarchyCategoriesWithin: {
        ofParent: {
          attributeCodeEquals: "vouchers-for-shareholders"
        }
      },
      entityLocaleEquals: cs_CZ
    }
  ) {
    recordPage {
      data {
        primaryKey
        attributes {
          code
          name
        }
      }
    }
  }
}