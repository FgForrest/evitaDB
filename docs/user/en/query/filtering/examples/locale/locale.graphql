{
  queryProduct(
    filterBy: {
      hierarchyCategoriesWithin: {
        ofParent: {
          attributeCodeEquals: "vouchers-for-shareholders"
        }
      },
      entityLocaleEquals: en_US
    }
  ) {
    recordPage {
      data {
        primaryKey
        attributes {
          code
          name
        }
      }
    }
  }
}