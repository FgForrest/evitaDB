query(
    collection('Category'),
    filterBy(
        hierarchyWithin(
            attributeStartsWith('code', 'wire')
        )
    ),
    require(
        entityFetch(
            attributes('code')
        )
    )
)