{
  queryProduct(
    filterBy: {
      attributeBatteryCapacityLessThan: "125"
    }
  ) {
    recordPage {
      data {
        primaryKey
        attributes {
          code
          batteryCapacity
        }
      }
    }
  }
}