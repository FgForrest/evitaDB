query {
	listBrand(
		filterBy: {
			attributeNameStartsWith: "A"
			entityLocaleEquals: en
		}
		orderBy: {
			attributeNameNatural: ASC
		}
	) {
		primaryKey
		type
		allLocales
		attributes {
			name
		}
	}
}